- name: Drop logind drop-in to configure closing the lid as suspend-then-hibernate
  become: true
  ansible.builtin.copy:
    src: logind/00_suspend-then-hibernate.conf
    dest: "{{ etc_dir }}/systemd/logind.conf.d/"
    mode: '0644'
